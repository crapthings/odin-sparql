# SubSelect final VALUES gate

This local, offline gate verifies the SPARQL SubSelect production's one final
optional `VALUES` clause through parser, algebra, evaluator, and the external result
comparator. It is a release-gated grammar/semantic check, not a W3C manifest
claim.
